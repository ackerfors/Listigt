<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.chalmers.dat255.listigt"
    android:versionCode="2"
    android:versionName="Listigt version 0.4.0" >

    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".MainListActivity"
            android:label="Your lists" 
            android:theme="@style/ListigtTheme">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ListEditor" android:theme="@style/ListigtTheme" android:label="Edit your list"></activity>
        <activity android:name=".ShareListActivity" android:theme="@style/ListigtTheme" android:label="Share your list"></activity>
        <activity android:name=".MainItemActivity" android:theme="@style/ListigtTheme.Items" android:label="Your items"></activity>
        <activity android:name=".ItemEditor" android:theme="@style/ListigtTheme.Items" android:label="Edit your item"></activity>
        <activity android:name=".ItemDetailsActivity" android:theme="@style/ListigtTheme.Items" android:label="Details"></activity>
    </application>
</manifest>